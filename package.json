{
  "name": "cx-automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cypress:open": "npx cypress open --e2e --browser chrome",
    "cypress:run": "npx cypress run --browser chrome",
    "test": "npx cypress run && node sendReport.js",
    "invoice": "cypress run --spec 'cypress/e2e/invoice/**/*.cy.js' --browser chrome --headed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jquery": "^3.7.1",
    "mochawesome-merge": "^5.0.0"
  },
  "devDependencies": {
    "cypress": "^14.3.1",
    "cypress-iframe": "^1.0.1",
    "cypress-mochawesome-reporter": "^3.8.2",
    "cypress-plugin-tab": "^1.0.5",
    "cypress-real-events": "^1.14.0",
    "cypress-xpath": "^2.0.1",
    "diff": "^7.0.0",
    "mochawesome": "^7.1.3"
  }
}
